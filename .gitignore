# ── Python ──
.venv/
.venv*/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.spec
.pytest_cache/
.mypy_cache/
.ruff_cache/
coverage.xml

# ── IDE / 编辑器 ──
.vscode/
.idea/
Session.vim
*.swp
*.swo
*~

# ── 本地配置（不入仓）──
CLAUDE.md
MEMORY.md
.claude/
*.local.yaml
*.local.json

# ── OS 产物 ──
.DS_Store
Thumbs.db

# ── MediaPipe 模型（首次运行自动下载）──
*.task

# ── 可执行文件（启动器 exe 独立维护）──
*.exe

# ── 测试产物 ──
tmp_*/
*.bak
*.log
*.tmp
/test_*_temp.py
/test_*_flow.py
/test_ai.py

# ── 覆盖率 ──
.coverage
htmlcov/
.coverage.*

# ── 数据/归档（运行时生成，不入仓）──
/data/
/reports/
docs/archive/
*.csv

# ── spike 自动输出 ──
spike/s2_result.json
spike/s1_result.json
spike/results/**/*.json
spike/results/**/*.png

# ── brainstorm ──
.superpowers/

# ── 本地模型文件（手动下载，不入仓）──
/models/
scripts/models/
# 模型二进制文件（防止误提交）
*.gguf
*.bin
*.ggml

# ── 样本数据（不入仓）──
/samples/

# ── 历史归档文档（不纳入工作树）──
docs/archive/
docs/old_schemes/
docs/superpowers/

# ── OpenSpec 工作流 ──
/openspec/

# ── 跨平台行尾符锁定 ──
.gitattributes tracked in repo

# ── 敏感配置（含 API Key，不入仓）──
config.yaml
